const base = 5; // valor fixo da base

function calcularAreaTriangulo(altura) {
    const area = (base * altura) / 2;
    console.log(`A área do triângulo é: ${area}`);
}

// Chamando a função com uma altura (exemplo: 8)
calcularAreaTriangulo(8);
