const numeros = [2, 4, 8, 16];

let i = 0; // Começa no índice 0
let soma = 0; // Começa a soma em 0

while (i < numeros.length) {
    soma += numeros[i]; // Soma o número atual
    i++; // Vai para o próximo índice
}

console.log(`A soma dos números é: ${soma}`);



